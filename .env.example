# OpenCode Zen API key (optional — free models work without one, IP-rate-limited)
# Get one at https://opencode.ai/auth for higher rate limits
OPENCODE_API_KEY=

# Free model — works out of the box, no key required
ZEN_MODEL=deepseek-v4-flash-free
